#include <stdio.h>

int main()
{
    // int i = 34;
    // int *ptr = &i;

    // printf("Value of ptr is %u\n", ptr);

    //  ptr++; // it will increment by +4
    //  ptr--; //it will decremrnt by -4

    // printf("Now the value of ptr is %u\n", ptr);

    // float i = 3.4;
    // float *ptr = &i;

    // printf("Value of ptr is %u\n", ptr);

    //  ptr++; // it will increment by +4
    // ptr--; // it will decremrnt by -4

    // printf("Now the value of ptr is %u\n", ptr);

    // char i = '34';
    // char *ptr = &i;

    // printf("Value of ptr is %u\n", ptr);

    // ptr++; // it will increment by +1
    // ptr--; // it will decremrnt by -1

    // printf("Now the value of ptr is %u\n", ptr);

    return 0;
    
}
